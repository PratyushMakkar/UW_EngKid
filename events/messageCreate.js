module.exports = {
  name: "messageCreate",
  async execute(message) {
    // const content = new MessageContent(
    //   message.channelId,
    //   message.author.id,
    //   message.content,
    //   message.createdTimestamp
    // );
  },
};
